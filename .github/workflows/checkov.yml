name: "Checkov [Honest Internal Workflow]"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  main:
    name: "checkov-internal-job"
    uses: honestbank/workflows/.github/workflows/checkov.yaml@main
    secrets: inherit
