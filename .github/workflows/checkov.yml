name: "Checkov Repository Workflow"
permissions: read-all

on:
  pull_request:
    branches:
      - main

jobs:
  main:
    name: checkov-repository-job
    uses: honestbank/workflows/.github/workflows/checkov.yaml@main
    secrets: inherit
